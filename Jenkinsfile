node {
    stage 'Checkout'
    checkout scm

    // def tagName = gitTagName()
    // echo "Tag Name: $tagName"

    stage 'Docker build'
    // @TODO use 'inside' to provide variables
    echo "building"
}
